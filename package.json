{
  "name": "rich-lcd-landing",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist/assets && cp index.html dist/index.html && cp -r assets/* dist/assets/",
    "dev": "npx serve ."
  }
}
